# requirements.txt
"""
Additional requirements for imitation learning approach
Add these to the main requirements.txt or install separately
"""

# Core ML libraries (if not already installed)
torch>=1.11.0
scikit-learn>=1.0.0
matplotlib>=3.5.0

# For data processing
pickle-mixin>=1.0.2
